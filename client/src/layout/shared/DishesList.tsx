import {FC} from "react";
import {DishesListPropsType} from "../../types/dishes.types";

export const DishesList: FC<DishesListPropsType> = props => {
  return (
    <div className={"dishes-list-container"}>

    </div>
  );
}